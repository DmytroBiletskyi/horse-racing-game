# Horse Racing Game (Vue + TypeScript)


## Requirements

Before running the project, make sure you have the following installed:

- **Node.js** `>= 18`
- **npm** (comes with Node.js)

Check your versions:
```bash
node -v
npm -v
```

# Install dependencies
npm install

# Run the project in development mode
npm run dev

# Run tests
npm test